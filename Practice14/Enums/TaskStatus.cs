// created enum TaskStatus that keeps constant values (progress) to choose for Task
// this will be used as a private field in TaskItem class (to protect encapsulation rules)
// example: TaskStatus (Progress) - ToDo
public enum TaskStatus
{
    ToDo,
    InProgress,
    Testing,
    Done
}
